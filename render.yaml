services:
  - type: web
    name: youtube-channel-analysis
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: RUNS_DIR
        value: /app/runs
      - key: YOUTUBE_API_KEY
        sync: false
